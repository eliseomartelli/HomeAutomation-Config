# lovelace_gen

type: markdown
style: >
  ha-card {
    box-shadow: none;
    background: none;
    padding: 10px 0px;
    color: var(--primary-text-color);
    background-color: transparent;
    margin-top: 16px;
    border-radius: 0; 
    font-weight: bold;
    font-size: 14px;
  }
content: |
  <h2>{{ title }}</h2>
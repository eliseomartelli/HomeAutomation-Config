- platform: template
  sensors:
        time:
                value_template: >
                        {{ now().strftime("%Y-%m-%d  %H:%M") }}
        who_cleans:
                friendly_name: "Who Cleans?"
                value_template: >
                        {{ states.calendar.house_cleaning.attributes.description }}
